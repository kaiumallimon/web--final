// JavaScript Regular Expressions: Syntax, methods, and examples.

const text = "The rain in Spain";
if (/rain/.test(text)) {
  console.log("Match found!");
}
console.log(text.replace(/Spain/, "France")); 