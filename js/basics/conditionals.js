// JavaScript Conditional Statements: if, else, else if, switch.

// Example: Check voting eligibility
// In browser, use prompt. In Node.js, use readline-sync or similar for input.
let age = 20; // Example value; replace with: Number(prompt("Enter your age:"));

if (age >= 18) {
  console.log("You're eligible for voting.");
} else {
  console.log("You're not eligible for voting.");
} 